version: "3"
services:
  frontend:
    image: frontend
    volumes:
      - ./frontend:/frontend
    ports:
      - 9000:9000
    command: quasar dev
  backend:
    image: backend
    volumes:
      - ./backend:/backend
    ports:
      - 8081:8081
    command: air
