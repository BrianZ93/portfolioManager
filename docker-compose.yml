version: "3"
services:
  frontend:
    image: frontend
    volumes:
      - ./frontend:/frontend
    network_mode: host
    # ports:
    #   - 9000:9000
    command: quasar dev
  backend:
    image: backend
    volumes:
      - ./backend:/backend
    network_mode: host
    # ports:
    #   - 8081:8081
    command: air
